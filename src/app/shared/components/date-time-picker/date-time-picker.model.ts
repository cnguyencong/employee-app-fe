export interface CustomDateTimePickerProp {
  id? : string,
  label?: string,
  name: string,
  value: unknown,
  mode : string,
  min?: any,
  max?:any,
  placeholder?: string,
  format? : string,
  size? : string,
  disabled? : boolean
}
